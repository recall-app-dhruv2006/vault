// Vitest stub for the `server-only` package. The real package throws when
// imported from a browser bundle; in tests (a Node environment) it's a
// deliberate no-op, mirroring what happens when Next.js runs it server-side.
export {};
